# controller

서버 인스턴스의 애플리케이션 엔드포인트를 제공하는 웹 계층입니다. 수강 신청 처리, 상태 조회 등 외부 요청을 받아 서비스 계층으로 위임합니다.

## 주요 구성요소

- `ServerController`
  - 수강 신청/조회 관련 REST API를 노출합니다.
  - 요청 유효성 검증을 수행하고 `service` 계층을 호출하여 실제 비즈니스 로직을 실행합니다.

## 역할

- HTTP 요청 수신 및 응답 반환
- 입력 검증, 예외 처리(컨트롤러 범위)
- 서비스 계층과의 경계 역할 수행
